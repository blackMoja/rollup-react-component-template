import type { FC } from 'react';

const RedBox: FC = () => {
  return (
    <div
      id="red-box"
      style={{ backgroundColor: 'red', width: 500, height: 500 }}
    />
  );
};

export default RedBox;
